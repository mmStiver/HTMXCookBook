@page "{handler?}"
@model HtmxCookbook.Pages.ProgessBar.IndexModel
@{
}

<div hx-target="this" hx-swap="outerHTML">
    <h3>Start Progress</h3>
    <button class="btn" hx-headers='{"RequestVerificationToken": "@Model.RequestToken"}' hx-post="/ProgressBar/StartJob">
        Start Job
    </button>
</div>